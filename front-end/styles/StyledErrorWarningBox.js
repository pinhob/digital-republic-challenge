import styled from 'styled-components';

export const StyledErrorWarningBox = styled.section`
  background-color: var(--red);
  color: var(--white);
  padding: 1.5rem;
  

  h2 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  p {
    font-size: 1.2rem;
    font-weight: 600;
  }
`;